<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
<div style="background-color:white;" >
<% include partials/_header %>
</div>
<!-- <img src="/cat.jpg" alt="Cat not found" style="width:160px;height:120px;"> -->
<div style="background-color:white;" ><center> <h1>  Joao's Awesome URL Shortner </h1>
  <h2> List of your URLs</h2>
  <table style="width:55%">
</center></div>
    <tr>
      <td> <b> Short URL </b> </td>
      <td> <b> Long URL </b> </td>
    </tr>
    <% for (var shortUrl in urls[email]){ %>
    <tr>
      <td> <a href="<%= urls[email][shortUrl] %>"> <%= shortUrl %> </a> </td>
      <td> <a href="<%= urls[email][shortUrl] %>"> <%= urls[email][shortUrl] %> </a> </td>
      <td>
        <form method="GET" action= <%= `/urls/${shortUrl}/edit` %> >
          <input type="submit" value="Edit this URL">
        </form>
      </td>
      <td>
        <form method="POST" action= <%= `/urls/${shortUrl}/delete` %> >
          <input type="submit" value="Delete ♺">
        </form>
      </td>
    </tr>
    <% } %>
  </table>
  <table style="width:0%">
    <tr>
      <td>
        <a href="/urls/new"> Click here to shorten a new URL </a>
      </td>
    </tr>
  </table>
</body>
</html>
